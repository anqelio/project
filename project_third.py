from datetime import datetime, timedelta
try:
   if (brave == True) or  (leader and favorite_subject == ('Защита от тёмных исскуств')):
    print('Студент зачислен в Гриффиндор')
except ValueError as e:
    print('Некорректный ввод даты')